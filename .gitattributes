# Enforce LF line endings for cache CSV files to prevent phantom diffs
# across platforms (see #818)
cache/**/*.csv text eol=lf

# Provider artifacts are checksum-addressed and may legitimately contain CRLF
# records or terminal empty TSV fields. Preserve their bytes verbatim.
kb/hypotheses/**/*_artifacts/** -whitespace
