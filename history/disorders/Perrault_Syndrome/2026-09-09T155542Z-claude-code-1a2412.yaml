history_version: 1
target:
  kind: disorder
  slug: Perrault_Syndrome
  path: kb/disorders/Perrault_Syndrome.yaml
session:
  id: 2026-09-09T155542Z-claude-code-1a2412
  timestamp: '2026-09-09T15:55:42Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-sonnet-5
    agent_tool: claude-code
links:
  issues:
  - https://github.com/monarch-initiative/dismech/issues/9868
  prs: []
  urls: []
events:
- type: EDIT
  outcome: changed
  sections:
  - has_subtypes
  summary: Bind subtype_term MONDO IDs on PRLTS1-7 (#9868)
  details: |
    Added subtype_term (SubtypeDescriptor) bindings on all seven has_subtypes entries (PRLTS1-PRLTS7), reusing the MONDO IDs already present and OAK-verified in each subtype's mappings.mondo_mappings block. The coverage index (src/dismech/compare/mondo_priority.py:iter_covered_mondo_ids) only reads has_subtypes[].subtype_term.term.id and the disease-level mappings.mondo_mappings, not a mondo_mappings block nested inside a has_subtypes entry, so these seven MONDO IDs were previously invisible to MONDO coverage accounting and to the seed-stubs re-nomination skip. Verified with a direct call to iter_covered_mondo_ids that all seven IDs are now reported. Ran just validate, just validate-terms (offline, all seven CURIEs already cached with matching labels), just check-duplicate-keys, just check-entity-refs, just check-enum-values, and just validate-disorders — all pass. No content, evidence, or existing mondo_mappings blocks were changed.
