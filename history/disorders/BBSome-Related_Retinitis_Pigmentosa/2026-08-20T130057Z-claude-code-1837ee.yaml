history_version: 1
target:
  kind: disorder
  slug: BBSome-Related_Retinitis_Pigmentosa
  path: kb/disorders/BBSome-Related_Retinitis_Pigmentosa.yaml
session:
  id: 2026-08-20T130057Z-claude-code-1837ee
  timestamp: '2026-08-20T13:00:57Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues:
  - https://github.com/monarch-initiative/dismech/issues/4243
  prs: []
  urls: []
events:
- type: EDIT
  outcome: changed
  sections:
  - mappings
  summary: Add the RP74 (BBS2) per-locus MONDO anchor (#4243)
  details: |
    Closed the BBS2 half of the last open checkbox on issue #4243 (ciliary
    retinal-dystrophy lumping decision).

    Added a `mappings.mondo_mappings` entry for MONDO:0014692 (retinitis pigmentosa
    74) with `mapping_predicate: skos:relatedMatch`, placed alongside the
    `MONDO:1040048` BBS2-related-ciliopathy mapping and worded exactly parallel to
    the RP51 (`MONDO:0013274`, TTC8) and RP55 (`MONDO:0013312`, ARL6) per-locus
    anchors this entry already carried. BBS2 was already a supported locus in the
    entry's own description and its non-syndromic RP association was already
    evidenced twice from PMID:25541840; only the per-locus MONDO anchor was missing,
    which is precisely what the 2026-07-29 assessment on #4243 identified.

    Term identity verified directly: `uv run runoak -i sqlite:obo:mondo info
    MONDO:0014692 -O obo` gives label "retinitis pigmentosa 74",
    `intersection_of: RO:0004003 HGNC:967` (BBS2), `xref: OMIM:616562`, and
    `is_a: MONDO:1040048 ! BBS2-related ciliopathy`.

    No new evidence was added and no prose claim was strengthened - this is a
    mapping-anchor addition only.

    Validation run and read, not assumed:
    - `just validate kb/disorders/BBSome-Related_Retinitis_Pigmentosa.yaml` - passed
      (schema + terms + references), 83/83 snippets verified.
    - `just validate-disorders` over both changed files (the batched pass CI runs) -
      passed, 131/131 snippets verified against cached references.
    - `just check-duplicate-keys`, `just check-title-snippets` and
      `just check-term-cache-integrity` - all clean.

    The new row in `cache/mondo/terms.csv` and the `diseaseterm` enum cache row were
    written by linkml-term-validator via the repository's own PreToolUse validation
    hook, not by hand.
