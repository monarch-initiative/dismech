history_version: 1
target:
  kind: disorder
  slug: Long_QT_Syndrome
  path: kb/disorders/Long_QT_Syndrome.yaml
session:
  id: 2026-08-24T193903Z-claude-code-8e92eb
  timestamp: '2026-08-24T19:39:03Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues:
  - https://github.com/monarch-initiative/dismech/issues/9394
  prs: []
  urls: []
events:
- type: EDIT
  outcome: changed
  sections:
  - discussions
  summary: Migrate prose outcomes to supporting_outcome/refuting_outcome
  details: |
    Moved 2 prose value(s) out of `would_support` / `would_refute` into the new
    `supporting_outcome` / `refuting_outcome` slots (issue #9394).

    These values state what an experimental result *would be* -- "Increased progenitor
    apoptosis in patient organoids would establish that the mouse mechanism operates in
    human cells" -- rather than naming a node the result bears on. The two reference
    slots are documented as taking the `<kind>#<name>` hash-anchor grammar, so prose sat
    there unvalidated and rendered inside a monospace reference chip meant for a short
    token. The reference slots keep their references; only prose moved.

    The edit is a key rename: every block in the KB was homogeneous (370 all-reference,
    306 all-prose, 0 mixed), so no item line was touched and no value reflowed. Verified
    structurally against the pre-edit parse -- the moved set matches the prose set
    exactly, no reference was moved, and nothing outside the four keys changed.
