history_version: 1
target:
  kind: disorder
  slug: IFT140-related_Recessive_Ciliopathy
  path: kb/disorders/IFT140-related_Recessive_Ciliopathy.yaml
session:
  id: 2026-09-20T032135Z-claude-code-ec2463
  timestamp: '2026-09-20T03:21:35Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues:
  - https://github.com/monarch-initiative/dismech/issues/12199
  prs: []
  urls: []
events:
- type: EDIT
  outcome: changed
  sections:
  - has_subtypes
  summary: 'Genetics pass (wiring-010): bind IFT140 on both subtype rows'
  details: |
    Genetics pass for issue #12199 (epic #12189, d2g wiring-010).

    IFT140 (hgnc:29077) was already curated and already bound on the
    "IFT-A Retrograde Intraflagellar Transport Failure" node, so
    genetic[].mechanism_outlink was already 1/1. Both MONDO terms on the gap list
    (MONDO:0054708 retinitis pigmentosa 80, MONDO:0009964 short-rib thoracic
    dysplasia 9) were already grounded as `has_subtypes` rows.

    Change: added `genes:` (IFT140) to both `has_subtypes` rows, so the gene-defining
    claim each row's description makes is bound rather than left in prose.

    `genetic[].subtype` was deliberately left unset: IFT140 causes both subtypes, and
    `Genetic.subtype` is scalar, so setting it would assert a single-subtype
    association the entry does not make.

    Nothing was duplicated from kb/groupings/Ciliopathies.yaml or the ciliopathy
    module; this entry already declares
    `conforms_to: ciliopathy_dysfunction#Basal Body and Transition Zone Dysfunction`.

    No new references were needed.
