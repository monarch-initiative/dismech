history_version: 1
target:
  kind: disorder
  slug: Phenylketonuria
  path: kb/disorders/Phenylketonuria.yaml
session:
  id: 2026-08-18T030455Z-claude-code-b1190c
  timestamp: '2026-08-18T03:04:55Z'
  actors:
  - type: ai_agent
    name: claude-code
    agent_tool: claude-code
links:
  issues: []
  prs: []
  urls: []
events:
- type: EDIT
  outcome: changed
  sections:
  - pathophysiology
  - genetic_context
  summary: Migrate three free-text functional_impact values onto controlled values
  details: |
    Migrates all three free-text functional_impact strings onto controlled
    values, clearing part of a debt flagged when FunctionalImpactEnum was
    restructured:

      loss_of_function_or_hypomorphic            -> LOSS_OF_FUNCTION (the parent)
      complete_or_near_complete_loss_of_PAH_...  -> COMPLETE_LOSS_OF_FUNCTION
      partial_loss_of_PAH_activity               -> PARTIAL_LOSS_OF_FUNCTION

    These hand-rolled strings were the original evidence that the enum lacked a
    complete-loss value, so this entry both motivated the schema change and is now
    its worked demonstration.

    Also the worked NON-split case. Unlike COL1A1, PAH residual activity is a
    continuum whose class boundaries are clinical thresholds, so classic and mild
    PKU remain subtypes of one entry with different genetic contexts. The upstream
    node deliberately stays at the parent LOSS_OF_FUNCTION: it spans the whole
    allelic series, and PMID:21555948 describes a spectrum from classic through mild
    PKU to mild hyperphenylalaninemia, so resolving it further at that node would
    overstate what the genotype alone determines. The BH4-responsiveness quote is
    used as functional evidence of retained rather than abolished activity.

    Part of a tranche giving every FunctionalImpactEnum value a real, cited
    exemplar in the KB rather than only a prose example in the schema.

    Structural note found while doing this: the disease-level genetic: block uses
    the Genetic class, which has no functional_impact_category slot. The enum can
    only attach through Pathophysiology.genetic_context (or
    PhenotypeContext.genetic_context). That is why only 24 annotations existed
    KB-wide despite hundreds of genetic blocks, and it is a candidate follow-up.

    No new references were fetched for this tranche. Every genetic_evidence snippet
    is an exact quote already present and verified in the same entry, re-cited
    against the specific claim it supports, so the change adds precision without
    adding citation risk.

    Validation for the tranche: linkml-validate clean on all nine changed files;
    linkml-term-validator validate-data passes on the seven that gained
    ontology-bound content; reference_snippet_audit --strict reports 785/805
    snippets verified against cached references, with 20 skipped by prefix;
    check_duplicate_yaml_keys, check_snippet_length, check_title_snippets and
    check_empty_snippets all clean. check_folded_hyphens flagged one new finding,
    which was mine in the schema comment added by the preceding commit (a line
    ending in "disease-" would fold to "disease- contributing"); it was reflowed and
    the check is clean again.
