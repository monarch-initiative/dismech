history_version: 1
target:
  kind: disorder
  slug: Spinocerebellar_Ataxia_Type_1
  path: kb/disorders/Spinocerebellar_Ataxia_Type_1.yaml
session:
  id: 2026-09-06T083739Z-claude-code-9f6272
  timestamp: '2026-09-06T08:37:39Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues: []
  prs:
  - https://github.com/monarch-initiative/dismech/pull/11197
  urls: []
events:
- type: EDIT
  outcome: no_change
  summary: Second main-merge round on the SCA1 curation branch
  details: |
    Second branch-maintenance round on PR #11197. No change to the SCA1 entry
    itself; this record exists because the branch history changed again.

    main advanced another 3 commits and the PR conflicted on the same file as
    the first round, cache/enums/diseaseterm_f618932bb23f.csv. This is now a
    recurring pattern rather than a one-off: every merged curation PR appends
    its disease-term CURIE to that one shared enum cache, so any curation branch
    left open across a few merges will keep colliding there. The collision is
    benign in content, since the two sides append different CURIEs, but it does
    require a merge each time.

    Resolved the same way as the first round: merged origin/main rather than
    rebasing, because PR #11197 is authored by another account and a force-push
    would invalidate that checkout. git auto-merged the enum cache with no
    conflict hunk and MONDO:0008119 survives. No cache row was hand-written,
    reordered, or regenerated by hand.

    Post-merge review: the diff against the new origin/main is exactly this PR's
    own 49 files (the entry, three history records including this one, 39
    reference caches, the deep-research report plus citations and two artifacts,
    the enum cache line, and the deleted stub). No unrelated deletions, no stale
    reversions, no protected-path churn.

    Re-validated: just validate passes, snippets 95/95 verified, and the full
    twelve-gate set was re-run against the new origin/main in CI's exact command
    form rather than the more permissive just recipes.

    Unrelated to this merge and still outstanding: claude-review has now failed
    three times on three different commits (04178c1d8, 490db5b3c, 0e1df57b3),
    each time erroring after 0s with num_turns=1 and cost=$0, meaning the review
    agent never read the diff. The first two runs named an ai4c-reviewer usage
    limit resetting at 02:30 UTC; the third failed after that reset, so the
    reset was not the fix and a re-run alone does not help. This session lacks
    actions:write and cannot re-run the job (403 on both rerun-failed-jobs and
    workflow dispatch). All of this is recorded on the PR, including a
    correction to the earlier "just re-run it" advice. It needs a human to look
    at the reviewer bot's credentials or account.
