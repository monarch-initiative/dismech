history_version: 1
target:
  kind: disorder
  slug: Spinocerebellar_Ataxia_Type_1
  path: kb/disorders/Spinocerebellar_Ataxia_Type_1.yaml
session:
  id: 2026-09-06T041249Z-claude-code-b0e35e
  timestamp: '2026-09-06T04:12:49Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues: []
  prs:
  - https://github.com/monarch-initiative/dismech/pull/11197
  urls: []
events:
- type: EDIT
  outcome: no_change
  summary: Merge main into SCA1 curation branch
  details: |
    Branch maintenance round on PR #11197. No change to the SCA1 entry itself;
    this record exists because the branch history changed.

    main advanced 22 commits after the PR was opened and GitHub reported the PR
    as conflicted (mergeable_state went from "blocked" to "unknown", and a local
    merge-tree against the new main reported one file changed on both sides).
    The conflict was cache/enums/diseaseterm_f618932bb23f.csv: this branch
    appended MONDO:0008119 to the disease-term enum cache, and curation PRs that
    merged to main in the meantime appended their own CURIEs to the same file.

    Resolved by merging origin/main into the branch rather than rebasing. The
    branch was created in this session, but PR #11197 is authored by another
    account, so a merge commit keeps that checkout valid where a rebase and
    force-push would not. git auto-merged the enum cache without a conflict
    hunk, because the two sides appended in different regions of the sorted
    file; MONDO:0008119 survives in the merged result. No cache row was
    hand-written, reordered, or regenerated by hand.

    Post-merge review per the repo's refresh guidance: the diff against the new
    origin/main is exactly this PR's own 48 files (the entry, two prior history
    records, 40 reference caches, the deep-research report plus citations and
    two artifacts, the enum cache line, and the deleted stub). No unrelated
    deletions, no stale reversions, no protected-path churn. Every PMID cited by
    the entry was confirmed to have its cache file present in the commit.

    Re-validated against the new main, which matters because the merged commits
    include a title-snippet baseline refresh (#11208) and a page/app-data
    regeneration (#11211). just validate passes; snippets 95/95 verified. The
    ref-based gates were re-run in CI's exact form against the new origin/main
    rather than the more permissive just recipes: check_snippet_length,
    check_title_snippets, check_reference_titles, check_folded_hyphens. The
    whole-KB gates were re-run too: check_duplicate_yaml_keys, check_enum_values,
    check_entity_refs, check_causal_targets, check_qualifier_terms,
    reference_cache_frontmatter, term_cache_integrity, dismech-stubs check.

    Unrelated to this merge, claude-review remains red on PR #11197 from a
    pre-reset ai4c-reviewer usage-limit error ("You've hit your limit · resets
    2:30am (UTC)", num_turns=1, cost=$0, so the agent never read the diff). It
    failed identically on two commits. This session lacks actions:write and
    cannot re-run it; both rerun-failed-jobs and workflow dispatch return 403.
    That is recorded on the PR and needs a human with that permission.
