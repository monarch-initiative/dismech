history_version: 1
target:
  kind: disorder
  slug: Babesiosis
  path: kb/disorders/Babesiosis.yaml
session:
  id: 2026-08-04T160507Z-claude-code-728426
  timestamp: '2026-08-04T16:05:07Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues:
  - https://github.com/monarch-initiative/dismech/issues/7288
  prs: []
  urls: []
events:
- type: EDIT
  outcome: changed
  sections:
  - references
  - evidence
  summary: 'Prefix 18 unprefixed http(s) references with url: CURIE'
  details: |
    Reference-formatting fix only; no curated claim, snippet, or ontology term changed.

    18 references (16 evidence items plus 2 top-level PublicationReference entries) cited two
    CDC clinical pages as bare URLs rather than `url:https://...`. Unprefixed, they resolve to
    the validator's UNKNOWN prefix and are never snippet-checked; the neighbouring entry in the
    same top-level references block already used the prefixed form (`clinicaltrials:NCT01528449`).

    Both target pages were already present in references_cache/ under their `url_` cache names,
    so this is a pure formatting correction -- no new fetch, and all 18 snippets were confirmed
    present in the existing cached text before the change was applied.

    Verified: linkml-validate (Disease) clean; reference snippet audit 150/156 verified against
    cached references (6 skipped by dataset prefix, unchanged by this edit).
