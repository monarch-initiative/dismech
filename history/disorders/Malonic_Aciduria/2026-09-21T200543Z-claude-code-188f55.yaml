history_version: 1
target:
  kind: disorder
  slug: Malonic_Aciduria
  path: kb/disorders/Malonic_Aciduria.yaml
session:
  id: 2026-09-21T200543Z-claude-code-188f55
  timestamp: '2026-09-21T20:05:43Z'
  actors:
  - type: ai_agent
    name: claude-code
    model: claude-opus-5
    agent_tool: claude-code
links:
  issues: []
  prs:
  - https://github.com/monarch-initiative/dismech/pull/7591
  urls: []
events:
- type: REVIEW
  outcome: changed
  sections:
  - evidence
  summary: 'Shepherd: resolve main conflict, migrate retired PARTIAL grades'
  details: |
    PR Shepherd round. Resolved a seven-file cache-CSV conflict with origin/main as a strict row union (every main row preserved; branch contributed 1-2 new rows per file; zero deletions). Two rows carried competing labels: HP:0003236 and MONDO:0011996 were renamed upstream, and main's newer rows were verified against OLS as the canonical labels, so main's values were taken and the branch's pre-rename labels (now synonyms) dropped. The merge then exposed two supports: PARTIAL values retired by #10003 after this branch was cut. Graded individually rather than blanket-converted: pathophysiology[6] PMID:39069445 -> SUPPORT, because the node description already states that decompensation is not universal and the cohort quote is the observation behind that statement; treatments[0] PMID:22778304 -> REFUTE, because the quote is a negative structural-prevention result that the treatment description does not carry, matching the repo's established use of REFUTE for negative efficacy results. Both explanations were rewritten to drop the prose that argued for the retired grade. Validation: just validate clean (schema + terms + 68/68 snippets), duplicate-key, entity-ref, causal-target, qualifier-term, enum-value, cache-integrity, cache-order and case-collision gates all green.
