<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ enum_name }} | DisMech Classifications</title>
    <style>
        :root {
            --bg: #f8fafc;
            --card: #ffffff;
            --text: #0f172a;
            --text-muted: #475569;
            --accent: #2563eb;
            --border: #e2e8f0;
        }

        body {
            margin: 0;
            font-family: "Source Serif 4", ui-serif, serif;
            background: var(--bg);
            color: var(--text);
        }

        a {
            color: var(--accent);
            text-decoration: none;
        }

        .container {
            max-width: 980px;
            margin: 32px auto;
            padding: 0 20px 40px;
        }

        .breadcrumb {
            font-size: 0.9rem;
            color: var(--text-muted);
        }

        .header {
            margin: 16px 0 24px;
        }

        .header h1 {
            margin: 0 0 8px;
            font-size: 2rem;
        }

        .header p {
            margin: 0;
            color: var(--text-muted);
        }

        .tree {
            list-style: none;
            padding-left: 0;
            margin: 0;
        }

        .node {
            margin: 10px 0;
            padding: 10px 12px;
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 10px;
        }

        .node-row {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .node-name {
            font-weight: 600;
        }

        .node-key {
            font-size: 0.8rem;
            color: var(--text-muted);
        }

        .node-desc {
            margin-top: 4px;
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .linkout {
            font-size: 0.8rem;
            border-bottom: 1px dotted rgba(0, 0, 0, 0.2);
        }

        .children {
            list-style: none;
            padding-left: 18px;
            margin: 8px 0 0;
            border-left: 2px solid #e5e7eb;
        }

        .disorders {
            list-style: none;
            padding-left: 0;
            margin: 8px 0 0;
            display: grid;
            gap: 4px;
        }

        .disorders li {
            font-size: 0.95rem;
        }
    </style>
</head>
<body>
    {% macro render_node(node) %}
    <li>
        <div class="node">
            <div class="node-row">
                <span class="node-name">{{ node.title or node.name }}</span>
                {% if node.title and node.title != node.name %}
                <span class="node-key">({{ node.name }})</span>
                {% endif %}
                {% if node.meaning %}
                <a class="linkout" href="{{ node.meaning | curie_to_url }}" target="_blank">term</a>
                {% endif %}
            </div>
            {% if node.description %}
            <div class="node-desc">{{ node.description }}</div>
            {% endif %}
            {% if node.is_leaf and node.disorders %}
            <ul class="disorders">
                {% for d in node.disorders %}
                <li><a href="../disorders/{{ d.slug }}.html">{{ d.name }}</a></li>
                {% endfor %}
            </ul>
            {% endif %}
            {% if node.children %}
            <ul class="children">
                {% for child in node.children %}
                {{ render_node(child) }}
                {% endfor %}
            </ul>
            {% endif %}
        </div>
    </li>
    {% endmacro %}

    <div class="container">
        <div class="breadcrumb">
            <a href="../../app/index.html">Browse All</a> / Classifications / {{ enum_name }}
        </div>
        <div class="header">
            <h1>{{ classification_title or enum_name }}</h1>
            {% if classification_description %}
            <p>{{ classification_description }}</p>
            {% endif %}
            {% if enum_description %}
            <p style="margin-top: 8px; font-size: 0.95rem; color: var(--text-muted);">
                <strong style="font-weight: 600;">Enum:</strong> {{ enum_name }} — {{ enum_description }}
            </p>
            {% endif %}
        </div>

        <ul class="tree">
            {% for root in roots %}
            {{ render_node(root) }}
            {% endfor %}
        </ul>
    </div>
</body>
</html>
