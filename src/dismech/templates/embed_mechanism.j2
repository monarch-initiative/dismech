Disease: {{ disease_name }}
Mechanism: {{ mechanism.name }}
Description: {{ mechanism.description | default('') }}
{% if mechanism.cell_types %}
Cell types: {% for ct in mechanism.cell_types %}{{ ct.preferred_term | default(ct.term.label if ct.term else '') }}{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if mechanism.biological_processes %}
Biological processes: {% for bp in mechanism.biological_processes %}{{ bp.preferred_term | default(bp.term.label if bp.term else '') }}{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if mechanism.downstream %}
Downstream effects: {% for d in mechanism.downstream %}{{ d.target }}{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
