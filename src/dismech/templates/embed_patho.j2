Disease: {{ name }}
Category: {{ category | default('Unknown') }}
{% if parents %}
Parents: {{ parents | join(', ') }}
{% endif %}

Pathophysiology:
{% for p in pathophysiology | default([]) %}
- {{ p.name }}: {{ p.description | default('') }}
{% if p.cell_types %}  Cell types: {% for ct in p.cell_types %}{{ ct.preferred_term | default(ct.term.label if ct.term else '') }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{% if p.biological_processes %}  Biological processes: {% for bp in p.biological_processes %}{{ bp.preferred_term | default(bp.term.label if bp.term else '') }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{% if p.downstream %}  Downstream effects: {% for d in p.downstream %}{{ d.target }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{% endfor %}
{% if has_subtypes %}

Subtypes:
{% for st in has_subtypes %}
- {{ st.name }}: {{ st.description | default('') }}
{% endfor %}
{% endif %}
