Disease: {{ name }}
Category: {{ category | default('Unknown') }}

Phenotypes:
{% for p in phenotypes | default([]) %}
- {{ p.name }} ({{ p.category | default('General') }}): {{ p.description | default('') }}
  Frequency: {{ p.frequency | default('Unknown') }}
{% if p.diagnostic %}  Diagnostic: Yes{% endif %}
{% if p.phenotype_term and p.phenotype_term.term %}  HPO: {{ p.phenotype_term.term.label | default('') }}{% endif %}
{% if p.sequelae %}  Can lead to: {% for s in p.sequelae %}{{ s.target }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{% endfor %}
{% if biochemical_profile %}

Biochemical Profile:
{% for b in biochemical_profile %}
- {{ b.name }}: {{ b.direction | default('') }} {{ b.description | default('') }}
{% endfor %}
{% endif %}
