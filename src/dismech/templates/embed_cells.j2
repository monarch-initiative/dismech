Disease: {{ name }}
Category: {{ category | default('Unknown') }}

Cell Types and Anatomy:
{% for p in pathophysiology | default([]) %}
{% if p.cell_types %}
{{ p.name }}:
{% for ct in p.cell_types %}  - {{ ct.preferred_term | default(ct.term.label if ct.term else '') }}{% if ct.term %} ({{ ct.term.id }}){% endif %}
{% endfor %}
{% endif %}
{% if p.tissue_term %}  Tissue: {{ p.tissue_term.preferred_term | default(p.tissue_term.term.label if p.tissue_term.term else '') }}{% endif %}
{% endfor %}
{% if histopathology %}

Histopathology:
{% for h in histopathology %}
- {{ h.name }}: {{ h.description | default('') }}
{% if h.cell_type_term %}  Cell type: {{ h.cell_type_term.preferred_term | default(h.cell_type_term.term.label if h.cell_type_term.term else '') }}{% endif %}
{% if h.tissue_term %}  Tissue: {{ h.tissue_term.preferred_term | default(h.tissue_term.term.label if h.tissue_term.term else '') }}{% endif %}
{% endfor %}
{% endif %}
