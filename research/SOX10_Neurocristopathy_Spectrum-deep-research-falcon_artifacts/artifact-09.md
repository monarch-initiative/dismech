> No PMID-citable report can be generated in this run because the system retrieved no citable literature contexts.
>
> Artifacts produced for reuse:
> - `artifact-00`: retrieval log documenting failed searches
> - `artifact-02`: section-by-section evidence checklist
> - `artifact-04`: full report template for later population
> - `artifact-06`: minimum evidence needed to complete the report
>
> How to use them:
> - Start with `artifact-00` to confirm the retrieval failure pattern.
> - Use `artifact-02` to guide database and literature re-search by section.
> - Populate `artifact-04` once PMID-linked sources are available.
> - Use `artifact-06` to prioritize the minimum authoritative sources needed for a complete report.


*Blockquote: This blockquote explains that no PMID-citable report can be produced in the current session because no citable contexts were retrieved. It also lists the main artifacts created and how they should be used in a rerun.*
