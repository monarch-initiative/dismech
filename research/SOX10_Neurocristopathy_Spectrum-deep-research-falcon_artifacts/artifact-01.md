> In this run, no citable papers or context IDs were retrieved for **SOX10 neurocristopathy spectrum** using the available tools, so a PMID-cited research report cannot be generated from the current evidence set.
>
> Once retrieval is restored, the next steps should be to query: **OMIM**, **Orphanet**, **PubMed** (`SOX10 neurocristopathy spectrum`, `SOX10 Waardenburg Hirschsprung`, `SOX10 PCWH syndrome`, `SOX10 leukodystrophy neuropathy`), **ClinVar/ClinGen** for variant interpretation, **gnomAD** for allele frequencies, and **ClinicalTrials.gov** for any disease-specific or supportive-management studies.


*Blockquote: This blockquote documents that no citable context IDs were available in the current run and lists the highest-priority databases and queries to use once evidence retrieval is working again.*
