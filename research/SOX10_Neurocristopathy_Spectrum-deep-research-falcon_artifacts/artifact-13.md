> Due to tool retrieval failure in this session—**0 papers retrieved and no citable context IDs produced**—a properly cited report on **SOX10 Neurocristopathy Spectrum** cannot be generated from the available evidence outputs.
>
> For a rerun, use this starting package:
> - `artifact-00` — evidence retrieval status table
> - `artifact-02` — section-by-section evidence retrieval checklist
> - `artifact-04` — full placeholder report template
> - `artifact-06` — minimum evidence needed to complete the report
> - `artifact-11` — requirement-by-requirement status summary
>
> Once literature retrieval is restored, these artifacts can be used to quickly rebuild the report with PMIDs, URLs, publication dates, and abstract quotes.


*Blockquote: This blockquote gives a concise user-facing explanation that no cited report could be produced because the session returned zero papers and no citable contexts. It also identifies the key artifacts to use as the starting package for a rerun.*
