> **Session limitation**
>
> No citable literature contexts were retrievable in this session (**Paper Count=0**), so the requested research report with **PMIDs, URLs, publication dates, and abstract quotes** cannot be produced from the available tool outputs.
>
> Artifacts available for rerun support:
> - `artifact-00`: evidence retrieval status table
> - `artifact-02`: section-by-section evidence retrieval checklist
> - `artifact-04`: full placeholder report template for later population
> - `artifact-06`: minimum evidence needed to complete the report


*Blockquote: This blockquote records that no citable contexts were retrieved in the session, preventing a properly cited report. It also points to the key artifacts that document the retrieval failure and provide templates for rerunning the workflow.*
