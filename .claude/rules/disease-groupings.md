---
paths:
  - "kb/groupings/*.yaml"
---

# Disease groupings

When creating, editing, reviewing, or auditing a disease grouping, use the
`curate-grouping` skill. Inspect current peers under `kb/groupings/` instead of
maintaining a static grouping catalog in `CLAUDE.md`.
