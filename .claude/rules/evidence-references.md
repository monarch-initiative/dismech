---
paths:
  - "kb/**/*.yaml"
  - "research/**/*.md"
  - "references_cache/*.md"
---

# Evidence and references

When adding, editing, validating, or reviewing evidence, use the
`dismech-references` skill. Never create or hand-edit
`references_cache/*.md`; regenerate entries with `just fetch-reference <ID>`.
