---
paths:
  - "kb/modules/*.yaml"
---

# Mechanism modules

When creating or substantially editing a mechanism module, use the
`create-module` skill and read `docs/primers/modules-and-conformance.md`.
Discover current examples directly under `kb/modules/`; do not add a static
module catalog to `CLAUDE.md`.
