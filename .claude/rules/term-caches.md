---
paths:
  - "cache/**/*.csv"
---

# Term and enum caches

Treat term and enum CSVs as derived artifacts. Use the `dismech-terms` skill for
cache problems; never type, append, or hand-position rows. Re-derive term rows
through validation and normalize with `just normalize-cache`.
