--- a/src/boomer/search.py
+++ b/src/boomer/search.py
@@ -409,0 +410 @@
+    seen_solutions: set[frozenset[Grounding]] = set()
@@ -412,0 +414,9 @@
+        if n.satifiable:
+            # Different search paths can entail the same complete assignment.
+            # Count that solution once, including before the candidate-count cap.
+            # Key by fact index and truth value, never by probability: distinct
+            # assignments may legitimately have equal priors.
+            key = frozenset(n.selections)
+            if key in seen_solutions:
+                continue
+            seen_solutions.add(key)
